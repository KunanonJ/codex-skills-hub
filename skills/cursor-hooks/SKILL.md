---
name: cursor-hooks
description: >-
  Cursor hooks configuration reference. Contains the hooks.json defining
  lifecycle hooks (beforeSubmitPrompt, stop, sessionStart, sessionEnd,
  beforeShellExecution, afterShellExecution) used by superconductor and
  other integrations.
metadata:
  version: "0.1.0"
---
# Cursor Hooks Configuration

This skill contains the hooks.json configuration from Cursor, defining lifecycle hooks
that run at various points in the agent session.

See `hooks.json` in this directory for the full configuration.
